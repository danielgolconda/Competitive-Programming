#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>
#define lld long long int 
#define mod 1000000007
#define FIO ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
using namespace std;
//Checking bit is set or not 
int main() 
{
    FIO
    int n,k;
    cin >> n >> k;
    // Here n is number and k is the Bit to be checked
    if((n&(1 << k)) > 0)
    {
        cout<<"Set"<<endl;
    }
    else
    {
        cout<<"Not Set"<<endl;
    }
	return 0;
}
